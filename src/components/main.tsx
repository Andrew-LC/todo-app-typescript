function Main({ children }: any) {
    return (
        <div className="bg-bg min-h-screen w-screen p-4 flex flex-col items-center gap-4">
            {children}
        </div>
    );
}

export default Main;
